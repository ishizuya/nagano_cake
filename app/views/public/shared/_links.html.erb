<%- if controller_name != 'sessions' %>
<div class="mt-5">
    <h4 class="d-inline bg-light p-1">既に登録済みの方</h4>
    <p class="mt-3"><%= link_to "こちら", new_session_path(resource_name) %>　からログインしてください。</p>
  <% end %>

  <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
    <h4 class="d-inline bg-light p-1">会員登録がお済みでない方</h4>
    <p class="mt-3"><%= link_to "こちら", new_registration_path(resource_name) %>　から新規登録を行ってください。</p>
  <% end %>
</div>